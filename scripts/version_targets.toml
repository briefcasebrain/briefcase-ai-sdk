# Canonical version reconciliation map for release automation.
# Keep exactly one canonical target.

[[targets]]
id = "workspace"
path = "Cargo.toml"
pattern = '(?m)^(?P<pre>version = ")(?P<version>[^"]+)(?P<post>")$'
replacement = '{pre}{version}{post}'
canonical = true

[[targets]]
id = "pyproject"
path = "pyproject.toml"
pattern = '(?m)^(?P<pre>version = ")(?P<version>[^"]+)(?P<post>")$'
replacement = '{pre}{version}{post}'

[[targets]]
id = "python_init_version"
path = "briefcase/__init__.py"
pattern = '(?m)^(?P<pre>__version__ = ")(?P<version>[^"]+)(?P<post>")$'
replacement = '{pre}{version}{post}'

# No target for bindings/python/src/lib.rs: `__version__` there is
# env!("CARGO_PKG_VERSION"), and bindings/python/Cargo.toml sets
# `version.workspace = true`, so it already resolves to the canonical version.

[[targets]]
id = "python_binding_core_dep"
path = "bindings/python/Cargo.toml"
pattern = '(?P<pre>briefcase-core = \{ version = ")(?P<version>[^"]+)(?P<post>")'
replacement = '{pre}{version}{post}'

# The compat manifest the `briefcase` CLI ships: its briefcase_ai field is the SDK's own version and
# must track the canonical version. (oci_jj_image_tag is oci-jj's version and is intentionally NOT a
# target — it tracks the oci-jj release the SDK is pinned to.)
[[targets]]
id = "compat_briefcase_ai"
path = "briefcase/cli/stack/compat.json"
pattern = '(?P<pre>"briefcase_ai": ")(?P<version>[^"]+)(?P<post>")'
replacement = '{pre}{version}{post}'

# The npm controls package releases in lockstep with the SDK. The pattern is
# indentation-anchored to the top-level version key only.
[[targets]]
id = "npm_controls"
path = "js/controls/package.json"
pattern = '(?m)^(?P<pre>  "version": ")(?P<version>[^"]+)(?P<post>",)$'
replacement = '{pre}{version}{post}'

# The npm native binding releases in lockstep; the platform packages under
# npm/ and the optionalDependencies pins track the same version.
[[targets]]
id = "npm_native"
path = "bindings/node/package.json"
pattern = '(?m)^(?P<pre>  "version": ")(?P<version>[^"]+)(?P<post>",)$'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_optdep_linux_x64_gnu"
path = "bindings/node/package.json"
pattern = '(?P<pre>"@briefcase-ai/native-linux-x64-gnu": ")(?P<version>[^"]+)(?P<post>")'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_optdep_linux_arm64_gnu"
path = "bindings/node/package.json"
pattern = '(?P<pre>"@briefcase-ai/native-linux-arm64-gnu": ")(?P<version>[^"]+)(?P<post>")'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_optdep_darwin_x64"
path = "bindings/node/package.json"
pattern = '(?P<pre>"@briefcase-ai/native-darwin-x64": ")(?P<version>[^"]+)(?P<post>")'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_optdep_darwin_arm64"
path = "bindings/node/package.json"
pattern = '(?P<pre>"@briefcase-ai/native-darwin-arm64": ")(?P<version>[^"]+)(?P<post>")'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_optdep_win32_x64_msvc"
path = "bindings/node/package.json"
pattern = '(?P<pre>"@briefcase-ai/native-win32-x64-msvc": ")(?P<version>[^"]+)(?P<post>")'
replacement = '{pre}{version}{post}'


[[targets]]
id = "npm_native_platform_linux_x64"
path = "bindings/node/npm/linux-x64-gnu/package.json"
pattern = '(?m)^(?P<pre>  "version": ")(?P<version>[^"]+)(?P<post>",)$'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_platform_linux_arm64"
path = "bindings/node/npm/linux-arm64-gnu/package.json"
pattern = '(?m)^(?P<pre>  "version": ")(?P<version>[^"]+)(?P<post>",)$'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_platform_darwin_x64"
path = "bindings/node/npm/darwin-x64/package.json"
pattern = '(?m)^(?P<pre>  "version": ")(?P<version>[^"]+)(?P<post>",)$'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_platform_darwin_arm64"
path = "bindings/node/npm/darwin-arm64/package.json"
pattern = '(?m)^(?P<pre>  "version": ")(?P<version>[^"]+)(?P<post>",)$'
replacement = '{pre}{version}{post}'

[[targets]]
id = "npm_native_platform_win32_x64"
path = "bindings/node/npm/win32-x64-msvc/package.json"
pattern = '(?m)^(?P<pre>  "version": ")(?P<version>[^"]+)(?P<post>",)$'
replacement = '{pre}{version}{post}'
